name: Deploy ORKIVAR main branch to dev and prod
run-name:  Deploy ORKIVAR main branch to dev and prod by @${{ github.actor }}

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy_prod_and_dev
  cancel-in-progress: true

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml
    secrets:
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}

  dev:
    needs: build
    name: Deploy to dev
    uses: ./.github/workflows/deploy.yml
    secrets:
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
    with:
      env: dev
      image: ${{ needs.build.outputs.image }}

  prod:
    name: Deploy to prod
    needs: build
    uses: ./.github/workflows/deploy.yml
    secrets:
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
    with:
      env: prod
      image: ${{ needs.build.outputs.image }}
